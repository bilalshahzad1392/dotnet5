@model List<WebApplication2.Models.UserModel>
 
@{
    Layout = "_MyLayout";

}

@{ 
    for(int i = 0; i < Model.Count; i++)
    {
        var user = Model[i];
        <div>Name: @user.Name - @user.ID</div>
    }
}